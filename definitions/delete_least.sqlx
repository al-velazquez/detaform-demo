config {
  type: "operations", // Creates a view in BigQuery. Try changing to "table" instead.
  description: "Description of the table",
  tags:["favorite"],
  //dependencies:["source"]
}

delete from dataform.source where count =(select min(count) from ${ref("source")})